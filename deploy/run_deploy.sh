#!/bin/bash
# Скрипт для Linux/macOS - полное развертывание системы

echo "🚀 Запуск развертывания системы аренды оборудования..."
echo

# Переходим в корневую папку проекта
cd "$(dirname "$0")/.."

# Запускаем главный скрипт развертывания
python3 deploy/deploy_all.py

# Пауза для просмотра результата (только если запущено интерактивно)
if [ -t 0 ]; then
    echo
    read -p "Нажмите Enter для продолжения..."
fi
